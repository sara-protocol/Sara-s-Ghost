# Tools & Snippets

This directory contains **auxiliary artifacts** used around Sara’s Ghost,
such as prompt bundles or website text snippets.

These tools:
- do not define system behavior,
- do not override protocols,
- do not change boundaries.

If a tool conflicts with core governance or safety rules,
the tool must be discarded.
